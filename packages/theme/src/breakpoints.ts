/**
 * Responsive breakpoints.
 *
 * These values are mirrored in postcss.config.base.cjs at the repository root so
 * CSS media queries and the JavaScript theme agree. Change both together.
 */
export const breakpoints = {
  xs: '36em',
  sm: '48em',
  md: '62em',
  lg: '75em',
  xl: '88em',
};
