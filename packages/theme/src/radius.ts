/**
 * Corner radius scale.
 *
 * `defaultRadius` in theme.ts pins the repository-wide default so individual
 * components never need to opt in.
 */
export const radius = {
  xs: '0.25rem',
  sm: '0.375rem',
  md: '0.5rem',
  lg: '0.75rem',
  xl: '1rem',
};

export const defaultRadius = 'md';
