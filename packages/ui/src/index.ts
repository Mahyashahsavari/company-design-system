/**
 * @monosuite/ui — provider only.
 *
 * Mantine components are imported from `@mantine/core` in apps and Storybook.
 * This package exists solely to apply `@monosuite/theme` consistently.
 */

export { MonosuiteProvider } from './provider';
export type { MonosuiteProviderProps } from './provider';
